final_df["DUP_KEY"] = (
    final_df["TVA"].fillna("").astype(str) + "|" +
    final_df["GSM"].fillna("").astype(str) + "|" +
    final_df["TEL"].fillna("").astype(str)
)

before = len(final_df)

final_df = final_df.drop_duplicates(subset=["DUP_KEY"], keep="first")

after = len(final_df)

print("Rows before:", before)
print("Rows after:", after)
print("Duplicates removed:", before - after)
