df["_TVA_CHECK"] = (
    df["TVA"]
    .astype(str)
    .str.strip()
    .str.upper()
    .str.replace(r"\.0+$", "", regex=True)
    .str.replace(r"^BE", "", regex=True)   # Remove BE at beginning
    .str.replace(r"\D", "", regex=True)    # Keep digits only
)
