cp = (
    df["CP"]
    .fillna("")
    .astype(str)
    .str.strip()
    .str.replace("\u00A0", "", regex=False)      # remove invisible space
    .str.replace(r"\.0$", "", regex=True)        # 4000.0 -> 4000
)

Replace this:

not_empty = (
    (cp != "") &
    (cp.str.lower() != "nan") &
    (cp.str.lower() != "none")
)

with this:

not_empty = (
    (cp != "") &
    (cp.str.lower() != "nan") &
    (cp.str.lower() != "none") &
    (cp.str.lower() != "nat")
)
